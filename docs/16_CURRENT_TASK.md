# CURRENT TASK

**TASK 006 — Sprint 3: Game Engine (Phase 5). ACTIVE.**

Approvals received 2026-07-18: Commit 1 (`docs/06_ENGINE_ARCHITECTURE.md`) approved — **sanctioned behavior deltas D1–D5 (§7.1) are ratified**; **vitest devDependency approved** (E1).

Commit 2 (E1) is COMPLETE and approved (2026-07-18): vitest harness + 26 characterization tests.

Commit 7 (E6 — EngineDriver + projections + snapshots; **the final extraction step**) is COMPLETE (2026-07-19), awaiting review/approval. **The engine extraction is complete**: `AbyssAnchor.tsx` is a UI shell (419 lines; 507 pre-E6, 1,415 pre-Sprint-2).

- `EngineDriver` (`src/engine/driver.ts`, §13): owns the rAF loop and the wall clock — the single sanctioned `requestAnimationFrame` site in the engine (§4). Per frame: wall delta → `engine.advance()` → sink.frame(`getRenderState()`, `getRenderTime()`) → sink.sync(`getPublicState()`). The driver+engine pair is owned per game session at **module scope** (§13): detach (unmount) pauses the loop and keeps all engine state; re-attach resumes with a fresh wall-clock baseline — remounting the component no longer restarts the world (the engine-side twin of the renderer's docs/05 §2.3 fix).
- Final §13 projections: `getRenderState()` — byte-compatible docs/05 §4 shape; `shipImpact.elapsed` is now `simTime − impactAt` on the simulation clock (§8; `impactAt` recorded at entry(impact)); the component's `performance.now()` FX bookkeeping is deleted; the FX *lifetime* cutoff stays renderer-owned (docs/05 R5 — the Effects layer no-ops past `SHIP_IMPACT_FX_SECONDS`), so the field stays non-null until the round's world reset (visually identical, renderer output byte-equal). `getRenderTime()` — `animTime = simTime`, `frameDt` = the clamped last wall delta fed into `advance()` (the engine still never reads a clock). `getPublicState()` — phase (fixed §5 projection to renderer strings), roundId, countdownSeconds, multiplier, boost, participant, chests, chosenChestId.
- `snapshot()`/`restore()` (§14): `EngineSnapshot { schemaVersion, tickCount, round: RoundSnapshot, world: WorldSnapshot }` — plain JSON data, deep-copied on both snapshot and restore (never aliases live state). Restore is a pure state assignment: no entry actions, no events; pending commands and accumulated wall time are discarded (they belong to the abandoned timeline); an unknown `schemaVersion` is rejected via `engineError` with state untouched (§18 ring 1). RNG stream positions are deliberately absent (§14's "seeded mode" field — both streams are unseeded Math.random until Phase 8); the Authority/Public secrecy split stays latent (§14: one process today), the shape is the authority view.
- **Sanctioned delta D5 lands here** (the last of the five): the component's 100 ms countdown display interval is deleted (`COUNTDOWN_TICK_MS` removed from `src/game/constants.ts` with its pin); the HUD mirrors engine public state once per frame from the driver's sync callback (phase, countdown, multiplier, boost — equal-value setState calls bail out, so quiet phases re-render nothing). Settlement listeners stay at the event edge, money math verbatim (§3); redundant display writes in listeners (setMultiplier at crashed/shipImpact/chestPicked) removed — the per-frame sync is authoritative and carries identical values in the same frame.
- Migration seams removed (§20 risk 2, closed): the E5 world-read getters (`worldY`/`boost`/`multiplier`/`creatures`/`countdown`) are gone from the facade — outside consumers read projections; the §7 clock constants left the barrel (engine-internal now); the component's rAF loop, `RenderState` assembly, `phaseRef`/`lastTsRef`/`shipImpactRef` are deleted. Input handlers submit commands and check only the wallet question (§3 affordability) — the engine validates everything else (D4).
- Suite now **81 tests green** (15 new: driver loop/attach/detach/clamp semantics on a stubbed rAF host; RenderState/RenderTime/public-state projection pins incl. shipImpact on the sim clock; §14 snapshot pins — round-trip, JSON losslessness, no-aliasing both ways, restore-continuation determinism through a full jackpot round, discarded pending commands/wall time, schema-mismatch rejection; the §21 determinism spot-check now compares full snapshots). Lint improved to **9 + 6** (one pre-existing error left with deleted component code; two new findings in fresh code caught and fixed pre-commit); tsc clean; production build passes; dev-server SSR smoke test HTTP 200 with canvas markup. Manual gameplay pass: owner's review step.

Previous — Commit 6 (E5 — Simulation systems + command queue), COMPLETE and approved (2026-07-18):

- `DiveSimulation` (`src/engine/simulation.ts`): the world state (worldY, boost, multiplier, flat creature array + spawn cursor, §10) and the §6.3 diving pipeline moved **verbatim** from the component's `stepSimulation` closure — multiplier → boost decay + bonus → crash check (terminal) → descent → spawner → collision → sea-floor check (terminal). The E3/E4 `divingTick` seam is deleted; the machine calls the simulation directly (world reset is now entry(diving)'s action). The creature id counter continues across rounds, as before.
- **Sanctioned delta D4 lands here**: `GameEngine.submit()` queues commands; each consumed tick drains the queue **before** stepping the machine — commands are validated against the state at the tick they apply (§13), never at submit time, and rejections emit `commandRejected` (§18 ring 1). `placeBet` (reasons: not-in-betting / already-participating / invalid-amount), `cashOut` (not-diving / not-participating / already-cashed-out; uses the **tick-authoritative multiplier**, replacing the component's stale render read), and `pickChest` (queued now, pre-D4 synchronous seam deleted) all flow through it. Commands submitted by event listeners land in the next tick's batch (§12 no-reentrancy holds by construction).
- Participant round state (§2) moved into the machine: `{ betAmount, cashedOut, cashedOutAt } | null`, cleared at entry(betting), auto-cashed at the jackpot on entry(impact) (§6.5). The affordability check stays wallet-side in the component before submit (§3); the debit rides the new `betPlaced` listener, cashout settlement rides the `cashedOut` listener — money math verbatim, engine never touches balances. The bet input's value is still locked into `roundBetRef` at `diveStarted` (settlement-side), exactly as before.
- Component: `stepSimulation` and all world refs (`worldYRef`, `creaturesRef`, `nextSpawnAtRef`, `nextCreatureId`, `boostRef`) deleted; the renderer's `RenderState` fields (worldY, boost, creatures) and the HUD's diving multiplier/boost now pull engine state per frame (§12 pull model; the countdown's 100 ms display interval remains until D5/E6). Rendering and projection logic untouched. `AbyssAnchor.tsx` is now 507 lines (was 563). Engine barrel trimmed: domain functions no longer exported — the facade, command/event edges, seams and tuning are the whole public surface (§4).
- Suite now **66 tests green** (state-machine harness rewritten to drive the *real* pipeline on a deterministic world Rng with outcomes forced via the scripted authority's crash point; new `simulation.test.ts` pins the pipeline order, spawn-ahead window/jitter, goldfish→boost→multiplier-bonus chain, terminal exits, reset semantics; new command-queue tests pin D4 boundary semantics, submit-order application, all rejection reasons, and listener-submitted-command deferral; the §21 determinism spot-check now covers commands + world state). Lint at the **10 + 6** baseline (zero new findings); tsc clean; production build passes.

Previous — Commit 5 (E4 — Round state machine), COMPLETE and approved (2026-07-18):

- `RoundStateMachine` (`src/engine/state-machine.ts`): the five §5 engine states, `roundId`, and all lifecycle timers as **whole-tick counts** on the simulation clock (§8) — betting 300, crash banner 144, impact→chests 84, chest result 132. Entry actions consult the `RoundAuthority` (§16) and emit the §12 events. **Sanctioned deltas D1 + D3 land here**: phase timers and the multiplier's dive-elapsed time base (`diveStartedAt`/`engine.diveElapsed`) run on the simulation clock; the diving pipeline reports a terminal outcome ("crashed"/"seaFloor") and ends its tick — crash and sea-floor can never double-fire.
- Deleted from the component: `beginDive`, the `CRASH_BANNER_MS` / `SHIP_IMPACT_TO_CHESTS_MS` / `CHEST_RESULT_TO_IDLE_MS` setTimeout chains **and their phase re-check guards** (structural now — one pending timer, owned by the current state), the `bonusTriggered` one-shot flag, and `RunState` (bet → settlement-side `roundBetRef`). The 100 ms countdown interval remains **display-only** (reads `engine.countdown`; the transition is engine-owned) until D5 deletes it in E6.
- Settlement extracted from the transition code into §12 event listeners (crashed → history, shipImpact → jackpot credit, chestPicked → delta credit) — money math verbatim, engine never touches balances (§3). `cashOut` stays a direct callback (D4 is E5); `pickChest` calls the engine (pre-D4 immediate apply + synchronous dispatch, documented seam).
- Suite now **50 tests green** (transition-sequence tests on a seeded engine per §20 risk 1: exact tick durations, event payload pins, D3 no-refire, bonus-waits-forever, pickChest rejection reporting). Lint improved to **10 errors + 6 warnings** (19 pre-existing prettier errors left with the deleted code; zero new findings); tsc clean; production build passes.

Previous — Commit 4 (E3 — Engine skeleton), COMPLETE and approved (2026-07-18):

- `GameEngine` facade (`src/engine/game-engine.ts`): simulation clock (`simTime`, tick-derived, §8) + fixed 60 Hz tick accumulator with `MAX_TICKS_PER_ADVANCE = 3` catch-up cap, excess dropped (§7). **Sanctioned delta D2 lands here** — fixed-step integration replaces display-rate variable dt; `RenderTime.animTime` now reads `engine.simTime` (§8: the accumulated clamped clock is precisely simTime). The multiplier stays on the wall clock (D1 is E4's delta); the tick-body structure is unchanged (D3 is E4's); no state machine, no command queue, no settlement change.
- Typed `EventBus` (`src/engine/events.ts`, §12): queued emits, post-tick dispatch from `advance()`, listener-exception firewall, reentrant emits deferred to the next dispatch. The §12 event map shapes are finalized (payloads carry `roundId`); `EngineCommand` protocol shapes defined (`src/engine/commands.ts`, §13). Emitters land with their owners in E4/E5.
- Mid-migration seam (§20 risk 2): the per-tick simulation body stays in the component, injected as `deps.tick`, run once per fixed tick; the component rAF feeds wall deltas into `advance()` and renders per frame ("driven from the existing component rAF", §21 E3 — the EngineDriver proper is E6). `swayRef` deleted. The multiplier's wall-clock read moved from the rAF timestamp to `performance.now()` inside the tick (same clock, sub-ms difference).
- Suite now **45 tests green** (10 new: accumulator semantics incl. the E3-scope determinism spot-check — identical advance sequences ⇒ identical tick traces — and EventBus rules). Lint unchanged at the 29+6 baseline; tsc clean; production build passes.

Previous — Commit 3 (E2 — Domain consolidation), COMPLETE and approved (2026-07-18):

- Pure domain math moved verbatim into `src/engine/domain/` (`multiplier.ts`, `descent.ts`, `outcomes.ts`, `creatures.ts`, `tuning.ts`); `Rng` service (`src/engine/rng.ts`, §11) injected at every engine draw site; `RoundAuthority` seam with `LocalRoundAuthority` (`src/engine/round-authority.ts`, §16). The deferred `sampleJackpot` micro-task was folded into `domain/outcomes.ts` per §20 risk 5 (**owner sign-off received with the E2 approval**). `src/engine/index.ts` is the public surface until the E3 facade; the outcome distributions are deliberately not exported from it.
- §4 constants ownership resolved: gameplay/simulation tuning → `src/engine/domain/tuning.ts`; presentation constants → `src/rendering/constants.ts` (+ `BG_*` palette → `theme.ts`); world geometry (`CHAIN_MAX_DEPTH`, `ANCHOR_WORLD_OFFSET_PX`, `SHIP_WORLD_Y`) → `src/shared/world.ts`. `src/game/` keeps only UI-shell values (`QUICK_BETS`, `HISTORY_LIMIT`, flash timings, player defaults, `COUNTDOWN_TICK_MS`); `formatMultiplier`/`chainTier` → `src/game/presentation.ts` (user-facing strings stay outside the engine, §3). `src/lib/abyss-game.ts` is dissolved. Contract types: `Phase` + `RenderCreature` are now defined in `src/rendering/render-state.ts`; `CreatureKind` sits on the shared floor.
- The depth-banded creature kind table + attribute rolls moved to `rollCreature` (`src/engine/domain/creatures.ts`) and are now pinned (`creatures.test.ts`), as recorded here in E1. All 26 E1 pins migrated with identical golden values; suite is now **35 tests, all green**.
- Lint baseline is now **29 errors + 6 warnings** (E1's temporary `sampleJackpot` export warning removed with the move; 3 kind-table prettier errors left the component with the extracted code — new engine files are prettier-clean; the dissolved `abyss-game.ts` took 1 error with it). All remaining findings are pre-existing.

Next (must not begin until E6 is approved): **Sprint 3 is code-complete** — E6 was the final §21 extraction step. Remaining before close-out: owner's manual gameplay pass + E6 approval, then the Sprint 3 close-out/retrospective (mirroring Sprint 2's). Sprint 4 (per `docs/04_DEVELOPMENT_ROADMAP.md`) must not begin until then.

Standing rules:

- One extraction step per commit; behavior-preserving (modulo the ratified §7.1 deltas, each landing in exactly one identified commit); game playable after every commit.
- Per-commit validation gate (doc 06 §21): `tsc --noEmit` clean; lint at the recorded baseline (9 + 6 since E6; 10 + 6 E4–E5); **all tests green**; manual gameplay pass; console hygiene; from E3 onward, a seeded-engine determinism spot-check.
- Follow `docs/18_GIT_WORKFLOW.md`: verify → commit → push → wait for approval.
- `docs/05_RENDERING_ARCHITECTURE.md` remains the binding rendering contract (its §18 import rules and §21 validation still apply); `docs/06_ENGINE_ARCHITECTURE.md` is the binding engine contract — on any conflict with reality, the commit stops and the document is corrected first.
